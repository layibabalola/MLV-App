# Fixtures

Checked-in fixtures are intentionally lightweight.

Current state:
- Existing repo receipts under `C:/!Layi Wkspc/MLV-App/.claude/worktrees/festive-boyd/receipts/`
  are used by the seed console tests.
- Clip-based golden fixtures are planned but not yet checked in.
- `golden/` is reserved for stable reference hashes and rendered outputs.
- `gpu_job_result_provenance/` holds a bound and an unbound GPU job result manifest
  fixture for `tools/repo_hygiene/gpu_job_result_provenance.py`.
