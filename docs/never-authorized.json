{
  "schema": "mlv-app/never-authorized/v26",
  "ruled": "2026-09-06",
  "authority": "owner directive 2026-09-06: the owner is out of the loop; these are the only acts no lane, quorum, or adjudication can unlock",
  "enforcement_note": "Two hooks exist. The GLOBAL hook ~/.claude/hooks/check-continuity-boundaries.py is shared by every project on this machine, fails open, and is NOT modified by this plan (rounds 1-4 measured it: 3-4 DENY controls, 28 ALLOW falsifiers; since 06:00:09Z it guards its own directory behind the agent-bridge FREEZE marker). The PROJECT hook tools/hooks/mlv-never-authorized.py (authored in 0.05, landed by 0.1) is tracked, registered in .claude/settings.json via $CLAUDE_PROJECT_DIR, applied in the board root and in every lane worktree at or after 0.1's merge, fails CLOSED, and is tested in CI with one DENY case per row. enforced_after_0_1 names the project hook.",
  "scope_note": "No Claude hook is visible to codex exec (the Codex sandbox bounds WHERE a lane writes, never WHAT it does), so no Codex lane (sol, luna) is ever started with -AllowEdits: refused by Invoke-Lane.ps1, the wrapper, and the dispatcher. An editing Claude lane receives an EXPLICIT tool allowlist of exactly the matched tool names plus Read/Glob/Grep; ALL is forbidden. LIMIT: a hook matches tool-input TEXT; an interpreter one-liner or a script invocation performs an act without matching a pattern. The hook raises the cost of a never-authorized act; the worktree boundary, the receipt checks, GitHub's protection on master, and the cross-family review are the other layers.",
  "items": [
    {
      "id": "NA-1",
      "act": "git push --force / -f / --force-with-lease against a non-empty expected value / +refspec, filter-branch, filter-repo, or any history rewrite on fork or any URL containing layibabalola/MLV-App",
      "enforced_today": "GitHub branch protection on master (allow_force_pushes: false, allow_deletions: false, enforce_admins: true - live GET 2026-09-06T05:33Z, re-GET identical 06:30Z); prose-only for other refs and URLs",
      "enforced_after_0_1": "project hook (Bash/PowerShell pattern)",
      "also": "sol review item 3"
    },
    {
      "id": "NA-2",
      "act": "deleting, MOVING (mv, move, Move-Item, git mv), truncating (>, Clear-Content, Set-Content, Out-File, tee) or overwriting (Write tool) ledger, pen, receipt, or evidence content under .claude-state/{closeout,fleet-runs,coordination} or .closeout-evidence, or .claude/ANALYSIS_LOG.md - EXCEPT create-or-extend writes under $D/receipts/**, $D/queue.json, $D/lane-gh-capability.json, $D/digest/**, $D/*-resume-CURRENT.md and .claude-state/fleet-runs/**, where only delete/move or a shrinking overwrite is denied (archive with a stub instead of deleting) - a PATH permission, not a TOOL permission: a shell truncating write (>, Set-Content, Out-File, tee) cannot be proven non-shrinking and is DENIED on these paths even for a create, so receipt writes use the Write/Edit tool or an append (Add-Content, >>); 0.2's canonical compound is the one shell write these paths admit, and only because the dedicated act is evaluated first (O125); a Write to exactly $D/receipts/0.2-enable-preflight-input.json is the PRE-FLIGHT ARTIFACT act, the second dedicated act, evaluated before generic content attribution: allowed only at the board venue, only when the target is absent, and only when its complete content parses as exactly one hook-stdin object whose tool_name is PowerShell and whose command is the semantically valid canonical enable compound for the current six receipts; it writes evidence only and authorizes no delete; any other path, a worktree venue, an existing target, a malformed object or a non-canonical command is DENIED (S125); an existing artifact is never rewritten or deleted - the hub reads it, types its compound verbatim and re-probes the stored file (O178) ; FOUR further exceptions, matched exactly: (i) deleting $D/WORKSTREAM-LOOP-DISABLED is ALLOWED only while $D/receipts/0.18-roadmap-parity.json, the NEWEST execution-control receipt (execution-control-0.7.json when 0.7 landed a PR touching a hashed file, else execution-control-0.6.json; newest by recordedUtc), superseding every earlier one and carrying forward roadmapParityReceiptSha256, queueSha256 and productLiveCount 15, 0.4b-required-checks.json, 0.4c-demoted.json, 0.6-ratio-guard.json and 0.5-factory-frozen.json all exist and VALIDATE against the receipt schemas of plan 1.3 step 5 - required keys per producing step, and for every execution-control receipt a reviewedHeadSha (the PR head sol reviewed before the merge) AND a mergeSha (the post-merge commit the hashes were taken at) with a solVerdictPath whose JSON is verdict APPROVE at subject_sha == reviewedHeadSha, the step's fixed set hashed equal at both shas by the hub before the receipt is written, after git -C $R fetch fork and cat-file -e on both shas (S123/O170), recorded as fixedSetEqualityProof = '<reviewedHeadSha>=<mergeSha>:<sha256, by the canonical command plan 1.3 step 5 pins (one '<sha256>  <path>' line per hashes entry, whole lines sorted as strings, LF-joined, no trailing newline, UTF-8), of the fixed set as hashed at reviewedHeadSha (O175/O176), computed by the hub over a named artifact it writes FIRST - $D\\receipts\\<step>-reviewed-head-hashes.json = {recordedUtc, reviewedHeadSha, hashes}, never over the receipt itself (O180) ->' whose shape, sha binding and digest (recomputed from the receipt's own hashes, no git) the hook validates and without which the receipt is INVALID (O172), and a hashes key set exactly equal to the step's required set (S120), recordedUtc in the pinned notation, 64-hex lowercase hashes, 40-hex lowercase shas, path fields that exist on disk, roadmapParityReceiptSha256 equal to the sha256 of 0.18-roadmap-parity.json on disk, productLiveCount exactly 15; an empty object, a missing or empty key, a wrong type or a malformed value is INVALID and the refusal names the file and the key; the execution-control candidate set is exactly the six chain names execution-control-{0.1,0.35,0.4c-i,0.4b-i,0.6,0.7}.json, the selected one is exactly execution-control-0.7.json when a valid 0.7 exists else exactly execution-control-0.6.json, the parsed-newest valid chain receipt must be that selected one, and any other execution-control-*.json is INVALID and fails the exception closed (S118) AND $D/receipts/0.2-loop-enabled.json is ABSENT on disk AND the SAME tool input creates it BEFORE the delete - the canonical compound form $ErrorActionPreference = 'Stop'; $r = '<json>'; Set-Content -LiteralPath '<receipt>' -Value $r -NoNewline -Encoding utf8 -ErrorAction Stop; if ((Get-Content -LiteralPath '<receipt>' -Raw -ErrorAction Stop) -cne $r) { throw 'enable-receipt-write-verification-failed' }; Remove-Item -LiteralPath '<marker>' -ErrorAction Stop, from which the hook extracts the JSON literal and validates it SEMANTICALLY, not for presence: state = enabling; enabledUtc and recordedUtc carry the ONE pinned notation YYYY-MM-DDTHH:MM:SSZ (whole seconds, uppercase Z, no fraction, no offset); executionControlReceipt = the basename of the NEWEST valid execution-control receipt by recordedUtc, where every receipt's recordedUtc the hook reads carries that same notation, the order is that of the PARSED value, and a non-conforming stamp or an exact tie makes the newest undecidable so the exception fails closed (O141); a chain receipt that is present but invalid is never excluded - it makes the newest undecidable - and recovery from an undecidable set is a NON-shrinking Write-tool rewrite of the offending receipt carrying a conforming stamp - for a chain receipt other than the selected one, strictly between its predecessor's and the selected receipt's parsed stamps - and, for execution-control-0.1.json, which has no predecessor, strictly earlier than the parsed stamp of the earliest OTHER valid chain receipt on disk (O183) - never the moment of the write (O177) - and every required key, never a delete (O152); when the offending receipt is 0.18-roadmap-parity.json the same hub step also rewrites, non-shrinking, every chain receipt from 0.35 on to re-point roadmapParityReceiptSha256 while preserving queueSha256, productLiveCount and each receipt's ORIGINAL recordedUtc VERBATIM, so the chain's parsed order is unchanged and no tie can be introduced - the one amendment to 'carried forward unchanged' (O159/O169/O171); executionControlSha256 = the lowercase SHA-256 of that exact file, derived as (Get-FileHash -Algorithm SHA256 -LiteralPath <receipt>).Hash.ToLowerInvariant() because Get-FileHash returns uppercase and the comparison is case-sensitive (O145) (S112) - and which it matches ONLY in this fail-closed form (the leading $ErrorActionPreference = 'Stop', which alone was measured to fail closed in every class - read-only target, missing parent, directory target, unresolvable drive - plus -ErrorAction Stop on both acts and the read-back throw, retained as defence in depth, not because either was measured load-bearing: -ErrorAction Stop is measured inert in every class (in the rev-19 form, dropping it while keeping the read-back changes no outcome, and it alone leaves three of four classes open), the read-back carried the rev-19 form and is the only arm verifying the receipt's content; the contract is the pinned shape, not this rationale - S101/O128/O131/O136); a delete-only input, or a literal failing validation, is DENIED - and the hook recognises this canonical compound as ONE DEDICATED ACT, evaluated BEFORE its generic per-command attribution of verbs to paths, which would otherwise deny the compound because Remove-Item beside the receipt path reads as a delete under $D/receipts/** (O118, measured 8/8 against the pre-S99 hook): the exact canonical shape is the enable act, and it is ALLOWED only when the hook's own --project-dir argument (substituted by Claude Code from ${CLAUDE_PROJECT_DIR} in the registered command, so no tool call can set it; the environment variable of that name is measured absent from hook processes - O126), normalised, equals MLV_BOARD_ROOT - a worktree value or a missing/empty argument is DENIED, because a worktree lane's hook evaluates the same absolute board paths and would otherwise admit the compound once the six receipts exist, before the hub has verified $R or installed the task (S105); ANY OTHER input naming both paths is DENIED; the delete verb naming the marker in any other input is DENIED; the compound's value is authorization-binding, not atomicity - two statements are as interruptible as two tool calls, which is why the create comes first and an interruption fails closed - this is plan step 0.2 and no other act unlocks it; the enable is ONE-SHOT: once the receipt exists a re-armed marker cannot be deleted without a newly ratified authorization (S98/S99); (ii) a shrinking overwrite of $D/*-resume-CURRENT.md or $D/ignition/seat-*.md is ALLOWED only when a byte-identical copy under .claude-state/continuity/archive/ already exists and the stub names its sha256 - this is plan step 0.9; (iii) CREATING $D/WORKSTREAM-LOOP-DISABLED is ALWAYS ALLOWED by any tool or shell verb with no receipt condition - re-arming the emergency stop is never gated, only its delete is (i); (iv) writes - create, extend or amend, any length - to the four ratified manifest surfaces $D/DEFINITIVE-FIX-PLAN-*.md, $D/never-authorized.json, $D/Start-EditingLane.ps1 and $D/prompts/v2/** are ALLOWED only when the hook runs for a session rooted at the board (the hook's own --project-dir argument, normalised, equals MLV_BOARD_ROOT; a worktree value or a missing/empty argument means the exception does not apply - O126), because re-ratification (plan 1.3) and 0.2's interruption recovery both require them and no lane is ever rooted there (the wrapper refuses workdir-is-board-root); delete/move of a manifest surface stays DENIED; the guard is re-hash and re-ratify before any step runs against the amended manifest (O124)",
      "enforced_today": "prose-only",
      "enforced_after_0_1": "project hook (dedicated 0.2 enable act evaluated FIRST; then verb + path guard; Write/Edit path guard; shrink guard on the carve-out paths; exception (iv) venue test (--project-dir) on the manifest surfaces)",
      "also": "project-memory P1"
    },
    {
      "id": "NA-3",
      "act": "claude auth login|logout, codex login, or assignment (X=, export, $env:, set, setx) of any variable starting ANTHROPIC_, OPENAI_, CLAUDE_CODE_, or PERSISTENT-scope assignment (setx, [Environment]::SetEnvironmentVariable(..., 'User'|'Machine')) of the names CLAUDE_PROJECT_DIR, MLV_BOARD_ROOT, MLV_LANE_PROMPT, MLV_REQUIRED_CHECKS_SNAPSHOT and MLV_CLIP_CACHE_ROOT - a persistent write is inherited by every later hook process and moves the board root the hook judges paths against (O129); the read-only claude auth status is allowed (S106)",
      "enforced_today": "global hook, PARTIAL: claude auth; the exact names ANTHROPIC_API_KEY / ANTHROPIC_AUTH_TOKEN / CLAUDE_CODE_OAUTH_TOKEN in shell and $env: forms; setx",
      "enforced_after_0_1": "project hook (prefix match; codex login)"
    },
    {
      "id": "NA-4",
      "act": "opening any footage clip (*.mlv, *.MLV, or anything under \\\\bachelor\\mlv-agent\\cache) other than (1) tracked fixtures under <worktree>/tests/fixtures/clips/, which are always allowed, or (2) the ONE canonical absolute path on the card's CLIP_OR_NONE line, compared without following links, case-insensitively, as a full path; a different clip with the same basename is DENIED; CLIP_OR_NONE: none means fixtures only",
      "enforced_today": "prose-only",
      "enforced_after_0_1": "project hook (reads CLIP_OR_NONE from the prompt file named by MLV_LANE_PROMPT; with the var unset, any .mlv outside the fixtures is DENIED); the cache UNC prefix is read from MLV_CLIP_CACHE_ROOT, defaulting to the literal when unset",
      "also": "project-memory presence-in-a-cache-is-not-consent-to-use-20260904.md"
    },
    {
      "id": "NA-5",
      "act": "spend beyond Invoke-WorkstreamLoop -DailyBudget (derived from charged dispatch reservations) or any dispatch while WORKSTREAM-LOOP-DISABLED exists (created by the hub 2026-09-06T06:36:57Z; lifted ONCE by plan step 0.2, the LAST step of Phase 0, under NA-2 exception (i), which closes the moment $D/receipts/0.2-loop-enabled.json exists - S98)",
      "enforced_today": "Invoke-WorkstreamLoop.ps1 (kill switch once per cycle; budget row after return - race open)",
      "enforced_after_0_1": "unchanged; TOOL-LOOP-PLUMBING-1 closes the race"
    },
    {
      "id": "NA-6",
      "act": "weakening, skipping (QSKIP, early return), or deleting a test; removing a test from a .pro or workflow manifest; adding continue-on-error to a workflow - EXCEPT a MOVE, matched exactly: an edit removing a test step or job from one tracked workflow is ALLOWED only when the same tool call, or the same PR's paired edit, re-adds the byte-identical run: body under .github/workflows/; the hook's NA-6 predicate is NET removal across .github/workflows/*.yml, never per-edit (0.4c-i's step move)",
      "enforced_today": "prose-only",
      "enforced_after_0_1": "project hook (assertion-count diff guard on tests/**; QSKIP; .pro/.yml removal judged as NET removal across .github/workflows/*.yml, a byte-identical move allowed; continue-on-error)",
      "also": "sol review item 3"
    },
    {
      "id": "NA-7",
      "act": "writing into .factory/ or into any path outside BOTH the lane's worktree and C:\\!Layi Wkspc\\MLV-App, by any tool or shell redirection (this plan never writes under ~/.claude/, so no exception is needed)",
      "enforced_today": "global hook, PARTIAL: .factory/ on Write/Edit/NotebookEdit only",
      "enforced_after_0_1": "project hook (Bash-side path guard; Write/Edit path guard; board root read from MLV_BOARD_ROOT, defaulting to the literal C:\\!Layi Wkspc\\MLV-App when unset - only the test sets it, the wrapper never does, and a lane tool call cannot reach the hook process environment)"
    },
    {
      "id": "NA-8",
      "act": "mutating the owner's Windows graphics settings (UserGpuPreferences), power state (powercfg), or any act needing the laptop unplugged; the vocabulary is EXTERNAL_CAPABILITY_UNAVAILABLE(owner-machine-state)",
      "enforced_today": "prose-only",
      "enforced_after_0_1": "project hook (Set-ItemProperty/reg add UserGpuPreferences; powercfg)"
    },
    {
      "id": "NA-9",
      "act": "deleting or weakening branch protection on fork/master, or removing a context from required_status_checks. ONE exception, matched exactly: the canonical 0.4b body {strict:true, checks: 'Repo Hygiene Python (windows-latest)', 'Repo Hygiene Python (ubuntu-latest)', 'Windows GUI Pilot', 'Windows Product Oracles', 'Batch Compile', every app_id 15368} sent by set-required-checks.ps1 while $D/receipts/0.4a-batch-compile-falsifier.json (failingContext 'Batch Compile') and $D/receipts/0.4c-guardrail-move.json (conclusion success) exist and $D/receipts/0.4b-required-checks.json is ABSENT; sole removal 'Factory Bridge Regressions', sole addition 'Batch Compile'. ADDING a context in any other body is hub-decidable; every other removal or weakening is DENIED",
      "enforced_today": "prose-only",
      "enforced_after_0_1": "project hook (gh api -X DELETE|PATCH|PUT on branches/*/protection*: add-only bodies allowed, judged against the LAST non-empty row of $D/receipts/required-checks-live.jsonl (MLV_REQUIRED_CHECKS_SNAPSHOT; APPEND-ONLY: 0.05 appends the first row from a live GET, set-required-checks.ps1 appends its post-PATCH row, and a rewrite would shrink the file, which NA-2 denies) - with the file absent, unparseable, or its final row malformed every protection mutation is DENIED; the canonical body only under the receipt conditions); bounded by set-required-checks.ps1's own receipt gate"
    },
    {
      "id": "NA-10",
      "act": "a lane editing its own gate: any write (Write, Edit, NotebookEdit or a shell write) to .claude/settings.json, .claude/settings.local.json or tools/hooks/mlv-never-authorized.py under ANY root is DENIED unless the hook's own --project-dir argument, normalised, equals MLV_BOARD_ROOT - the hook is re-read on every tool call, so a lane's edit of its registration or of the script would take effect on its next call; hook evolution stays hub-authored at the board venue, where the hub's own subagents run (O129, hub extension). LIMIT unchanged: an interpreter one-liner is invisible to a text-matching hook; the layers behind this rule are the wrapper's hook-drifted and hook-unregistered refusals at dispatch and the sol review of every PR diff.",
      "enforced_today": "prose-only",
      "enforced_after_0_1": "project hook (venue test on the registration files and the hook script)",
      "also": "card-TOOL-HOOK-ENFORCE-1 NA-10 rows"
    }
  ]
}
