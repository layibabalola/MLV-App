# RATIFY: DEFINITIVE FIX PLAN rev 28 — adversarial verdict (Codex family), round 27

You are `sol`, the Codex adversarial verifier, under `codex exec` in a read-only sandbox with a shell. You are one of two
independent keys; the other is a Claude model you cannot see. The owner is NOT in the loop. Rounds 1-26 returned your
verdicts — DO_NOT_PROCEED through round 10, PROCEED_WITH_AMENDMENTS in rounds 11-12, DO_NOT_PROCEED again in round 13 on S93, PROCEED_WITH_AMENDMENTS in round 14, DO_NOT_PROCEED in the five confirmations on S98, S99, S101/S102, S104/S105 and S108, PROCEED_WITH_AMENDMENTS in round 20, DO_NOT_PROCEED in round 21 on S113/S114, round 22 on S118, round 23 on S120, round 24 on S123 and round 25 on S125/S126, then PROCEED_WITH_AMENDMENTS in round 26 (S1-S130); rev 28 claims to have applied every finding that reproduced, keeping the design change both keys judged sound in rounds 5 through 23: the enforcement hook is an MLV-App PROJECT hook (`tools/hooks/mlv-never-authorized.py`, tracked, registered in
`.claude/settings.json`, applied in every worktree), and the shared global hook is never modified. Your S45 and the Claude
key's O46 both assumed the global hook would be patched; the hub rejected that premise. Judge the replacement on its merits.

**The fourteenth round opened the plan's gate (0.0) with both keys returning no blocker; the two confirmations since returned S98 (replayable
delete) and S99 (a bare first delete the hook still allowed), and the current revision closes both: the enable is ONE compound tool input the hook
validates — create the enable receipt first, delete the marker second, delete-only DENIED, ONE-SHOT thereafter; the compound is FAIL-CLOSED (`-ErrorAction Stop` on both acts, a read-back verification between
them — S101) and the enable receipt is finalised by one strictly longer rewrite (S102). Exception (iv) admits writes to the four manifest surfaces only from a session rooted at the board (O124); the receipt
carve-out is a path permission, not a tool permission (O125); The enable act is venue-bound (the hook's `--project-dir` argument equals the board root — S105/O126; the environment variable is measured
absent from hook processes, so the venue rides on argv on all four registration surfaces) and 0.2's pre-flight probes the exact input against
the LIVE board through the decision-only hook (`MLV_HOOK_DRYRUN=1`, no fixture — S104/O127); the canonical compound opens with
`$ErrorActionPreference = 'Stop'` (O128, hub-measured: the prior form failed OPEN on an unresolvable drive); NA-3 covers persistent-scope
assignment of the five hook names and NA-10 the hook's own registration and script (O129). The hub is the pre-dispatch EXPORTER (fetched objects, `pr-<n>-checks.json`, the composed review) for the three PRs
reviewed before 0.35 delivers the exporter (S108). The compound's rationale now names the leading preference as the arm the guarantee rests on (O131) and every surface
that enumerates the register says NA-1..NA-10 (O132/O133). The enable literal is validated SEMANTICALLY (newest receipt name, its exact sha256, parseable timestamps — S112) and the
toolchain card names its evidence actor (S111). Every hub-run `gh` pins `-R layibabalola/MLV-App` (O135); the compound's retained arms are stated as measured — `-ErrorAction Stop`
inert, the read-back load-bearing in rev 19 (O136); every card enumerating the register reaches NA-10 (O137); the review template names the
pre-dispatch exporter (O138). The hub's own entry procedure `hub-procedure.md` joins the manifest this rev — audit it as a card. The exporter also exports the PR itself (`pr-<n>-review.json`: headRefOid, body, checks) and refuses on head drift (S114);
the procedure's lane rule matches 0.15 and the cards (S113); post-0.1 hook authoring happens in a worktree inside the board root (S116). Every `recordedUtc` the hook reads carries ONE fixed notation and the newest receipt is chosen by parsed value (O141 — a
SEVENTH hook delta); RESUME.md's pointer is a literal path verified with `Test-Path` (O143). INDEPENDENCE: do not read
`orchestrator-resume-CURRENT.md` or any `fleet-runs\ratify-*\` directory of the CURRENT round beyond `subject-manifest.txt`; they carry the other
key's finding ids. The hub procedure's read order is for the hub, not for you. The six gate receipts validate against ONE schema table (plan §1.3 step 5): required keys per producing
step, pinned stamps, well-formed hashes, existing paths, the control chain's six names only, 0.7-when-valid-else-0.6 (S118 — an EIGHTH hook delta). A present-but-invalid chain receipt makes the newest undecidable — never excluded — and recovery is a non-shrinking Write-tool
rewrite (O152); RESUME.md's checker asserts no control byte and every dual-lane pointer resolves, and RESUME.md carries its own repair
instruction (O149/O150); the pinned notation and its derivation are in the hub procedure's §3 (O151); PR_STEP has two ratified values (O153). Every control receipt binds sol's APPROVE at `reviewedHeadSha` and an exact per-step hash key set (S120); the exporter
exports the required-context set before and after the checks (S121); PR_STEP's two literals are complete in the template and BRANCH is a
full-card runtime value (S122). Composition is a fixed point and the one hand-composed card gets its literal branch (O157); the hash key set per step is
exact and 0.2 re-hashes that fixed set (O158); a rewrite of the parity receipt re-points every later chain receipt in the same step (O159); the
notation's derivation carries the invariant culture (O160); the receipt-write universal is scoped to the hub's own tool calls (O161). Every control receipt carries BOTH the reviewed PR head and the merge sha, sol's APPROVE bound to the head and the
fixed set proven equal at both before the receipt is written (S123); the procedure's receipt-write sentence is scoped exactly as the plan's (S124). The key set is stated once, by the table (O164); 0.7's receipt names its reviewed head, merge sha and verdict with a
selection rule (O165); the plumbing card's quoted placeholder is prose (O166); BRANCH is in the template's runtime list (O168); the cascade
preserves every original stamp (O169). The pre-flight artifact Write is a dedicated act (S125); the plumbing card carries the exporter deliverable (S126); the
roadmap row is derived from its last change (S127); every citation of the register's version equals its schema line (S128). Both shas are fetched and asserted present before the equality run (O170); the cascade's preserved stamp is on every
surface (O171); the equality leaves a proof the hook validates (O172); the plumbing card's bytes carry no placeholder (O173); 0.18's producer
matches its table row (O174). 0.7's producer no longer names a key its row lacks (S130). The proof digest is pinned to one canonical command and computed at the reviewed head (O175/O176); an O152
repair carries an order-preserving stamp (O177); an existing pre-flight artifact is re-probed, never rewritten (O178); the exporter's commands
match its consumers (O179). The Claude key's O118 (the compound denied by the pre-S99 hook's generic attribution) was
CONTESTED with S99 and resolved by the hub rejecting the premise both shared: the canonical enable is a DEDICATED act the hook recognises
before generic attribution runs — recorded in the round-16 receipt, re-ratified here. This round CONFIRMS the current
revision while 0.05 (hub authoring on a branch; nothing merged, nothing installed) continues in parallel. 0.1's merge and canonical-checkout move
wait for this round's result.**

## Subject = the MANIFEST hash: plan + register + wrapper + every prompts/v2/*.md + the DIAGNOSIS_BASE line
```
python -c "import hashlib,glob,os;D=r'C:\!Layi Wkspc\MLV-App\.claude-state\coordination\dual-lane';fs=sorted([D+r'\DEFINITIVE-FIX-PLAN-20260906.md',D+r'\never-authorized.json',D+r'\Start-EditingLane.ps1']+glob.glob(D+r'\prompts\v2\*.md'),key=os.path.basename);base='55c486d450188e57d3e4c2b7e036fc8c85005b34';m='\n'.join([hashlib.sha256(open(f,'rb').read()).hexdigest()+'  '+os.path.basename(f) for f in fs]+[base+'  BASE']);print(hashlib.sha256(m.encode()).hexdigest());print(base)"
```
Print both lines first. Your verdict binds to the manifest hash. Add `-c safe.directory='C:/!Layi Wkspc/MLV-App'` to git if
needed. **Derive at DIAGNOSIS_BASE** (`git show <sha>:<path>`). You MAY read `fleet-runs\loop-cycles\` and the twenty-three earlier rounds
`fleet-runs\ratify-e05b81cc-*\`, `ratify-4361880b-*\`, `ratify-98c9f6d2-*\`, `ratify-8c2b7503-*\`, `ratify-46219c7f-*\`, `ratify-c9ac61a7-*\`, `ratify-4f92ce36-*\`, `ratify-1e2b2576-*\`, `ratify-6288394f-*\`, `ratify-9ad73ce4-*\`, `ratify-d0a0397a-*\`, `ratify-fa889622-*\`, `ratify-53c2a498-*\`, `ratify-60a23298-*\`, `ratify-09a49ba0-*\`, `ratify-eb37db02-*\`, `ratify-749cef22-*\`, `ratify-1d3f286c-*\`, `ratify-9256eda7-*\`, `ratify-ff4b1dee-*\`, `ratify-60cb5363-*\`, `ratify-2fcf4ebd-*\`, `ratify-011f8954-*\`. Do NOT read any other
`fleet-runs\ratify-*` directory. Do not probe the deployed global hook (it appends a trace log); review sources and tables.
**State moved since round 4 and the plan says so:** `$D\WORKSTREAM-LOOP-DISABLED` now exists (hub, 06:36:57Z); verify with
`Test-Path` and the newest cycle receipt's `haltedReason`.

## Ratification scope (apply it; say a/b/c per finding)
(a) the plan's DECISIONS; (b) Phase 0's ORDERING and EXECUTABILITY — every precondition a FILE the next step reads, written by a
named actor who can write it; (c) the prompts' fidelity to (a). Card-internal defects a card's own acceptance or the per-PR sol
review would catch are `major`, not `blocker`, unless a Phase 0 step becomes unexecutable.

## Your job
1. **Round-26 closure audit**: S130, plus S125 (PARTIAL until the eleventh hook commit) and S129 (UNMEASURED — re-measure it), each `CLOSED` / `OPEN` / `PARTIAL` with the command re-run.
2. **The design change**: does a project hook registered via `$CLAUDE_PROJECT_DIR` in tracked `.claude/settings.json` actually
   apply to a `claude -p` lane started in a worktree (read the existing `.claude/settings.json` at BASE for the pattern this
   repo already uses, and this repo's rule that a hook's script must live on the same ref)? Does it remove the freeze-marker
   problem entirely? Does anything in the plan still write under `~/.claude/`? Is NA-2's carve-out (O47) sufficient for every
   receipt the plan writes — enumerate the receipt paths and test each against the rule text.
3. **Re-derive §0 at DIAGNOSIS_BASE.** Non-reproducing value → `major`; wrong direction → `blocker`.
4. **Phase 0 walk** in rev 28's actual order: 0.05 → 0.1 → 0.15 → 0.18 → 0.35 → 0.4a-i → 0.4a-ii → 0.4c-i → 0.4b-i →
   0.4b-ii → 0.4c-ii → 0.5 → 0.6 → 0.7 → 0.9 → **0.2 LAST** (former 0.3 no longer exists — 0.35 discharges it; 0.2 requires
   six receipts (incl. 0.5's), O73/S78; the execution-control receipt 0.2 names is `execution-control-0.7.json` when 0.7 writes it, otherwise `execution-control-0.6.json` — the NEWEST by recordedUtc, S81/O102; the install is bound to a freshly detached `$R`, S83; seeded cards carry `track == kind`, S82; the NA-9 snapshot is append-only JSONL, S84; 0.7 and 0.9 complete before 0.2, S87; the wrapper's registration check is an exact pinned-command match of a `type: command` entry with a case-sensitive matcher, S89/S92/S94; 0.7 writes `execution-control-0.7.json` when it lands a PR touching a hashed file and 0.2 requires the NEWEST receipt, O102). For each: the FILE that must exist before it, who writes it, whether the actor can act, whether it
   can run early. Check the wrapper resolves everything from `-WorkDir` and has no probe exemption. Check 0.2 is now LAST and requires six
   receipt files. For 0.1's checkout move derive `git merge-base --is-ancestor master fork/master` and the left-right count (O69).
5. **Cards**: every lane-dispatchable card has a file; no card reads `$D`, the queue, or `gh`; `{{PR_STEP}}` present where a
   PR is opened; `CLIP_OR_NONE` is `none` or one canonical absolute path; verify-first bound to `{{BASE_SHA}}`; no unknown
   composer labels. Find the next retraction if there is one.
6. **Declare one self-failure**; mark it `UNMEASURED`.

## Output — end with exactly this JSON block
```json
{"verdict": "PROCEED|PROCEED_WITH_AMENDMENTS|DO_NOT_PROCEED", "subject_manifest_sha256": "<hash>", "base_sha": "<sha>",
 "round26_closure": [{"id": "S130", "status": "CLOSED|OPEN|PARTIAL", "repro": "<command>"}],
 "findings": [{"id": "S131", "severity": "blocker|major|minor|unmeasured", "scope": "a|b|c", "section": "...",
               "claim": "...", "repro": "<command>", "amendment": "<exact text change or 'none'>"}],
 "self_failure": "..."}
```
`DO_NOT_PROCEED` requires at least one `blocker` with a reproducing command under scope (a) or (b).
